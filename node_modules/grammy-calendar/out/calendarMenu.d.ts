import { type Context, type MiddlewareFn, type MiddlewareObj, type InlineKeyboardMarkup } from "./deps.node.js";
import { type CalendarOptions, type CalendarContext } from "./types.js";
declare type CustomContext = Context & CalendarContext;
export declare class Calendar<T extends CustomContext = CustomContext> implements InlineKeyboardMarkup, MiddlewareObj<T> {
    private readonly extractOptions;
    constructor(extractOptions: (ctx: T) => Partial<CalendarOptions>);
    readonly inline_keyboard: never[];
    private prepare;
    middleware(): MiddlewareFn<T>;
}
export {};
